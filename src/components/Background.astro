---
---

<div class="container"></div>

<style>
  .container {
    height: inherit;
    position: absolute;
    width: inherit;
    background: url('./static/images/background-code.jpg') no-repeat center;
    background-size: cover;
  }

  .container::after {
    content: '';
    width: 100%;
    height: 100%;
    position: absolute;
    backdrop-filter: blur(2px) grayscale(1) invert(0);
  }

  @media (prefers-color-scheme: dark) {
    .container::after {
      backdrop-filter: blur(2px) grayscale(1) invert(0.95);
    }
  }
</style>
