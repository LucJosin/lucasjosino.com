---
import { getStaticTagPaths } from '@lib/blog';
import Page from '../../../blog/tags/[...tag].astro';

export const getStaticPaths = getStaticTagPaths({
  meta: import.meta.url,
});

const { props } = Astro;
---

<Page {...props} />
